List<int> numbers = [2, 1, 4, 3, 5, 10, 7, 8];
List<int> reversed = numbers.reversed.toList();
void main() {
// sort by order
  // numbers.sort();
  // numbers.sort((a, b) => b.compareTo(a));
  print(numbers);
}
